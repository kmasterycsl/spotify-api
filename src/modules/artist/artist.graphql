#import "../asset/asset.graphql"

type Query {
  artists: [Artist]
  artist(id: ID!): Artist
}

type Artist {
  id: Int!
  name: String!
  biography: String!
  isVerified: Boolean!
  coverImage: Asset!
  avatarImage: Asset!
}
